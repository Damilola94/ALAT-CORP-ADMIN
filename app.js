import { View, StyleSheet } from "react-native";

const ProductIem = ({ color }) => {
  <View style={[styles.view, { backgroundColor: color }]}></View>;
  <View style={{}}></View>;
};

const styles = StyleSheet.create({
  view: {
    backgroundColor: color,
  },
});
